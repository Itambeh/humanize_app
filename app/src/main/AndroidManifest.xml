<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.humanize">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_humanize"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_humanize_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Humanize">
        <activity android:name=".tela_dinheiro"></activity>
        <activity android:name=".tela_roupas" />
        <activity android:name=".donations" />
        <activity android:name=".cadastro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>